# Only needed if you host the client and server on separate origins.
# In local dev the client auto-connects to http://localhost:3001.
# In production (single service) it connects to the same origin automatically.
VITE_SERVER_URL=https://your-server-host.example.com
